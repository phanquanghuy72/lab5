@model IEnumerable<String>
    <div id="2">
        @foreach (var item in Model)
        {
            @Html.CheckBox(item);
            @Html.Label(item);
            <b>&nbsp;</b>
        }
    </div>
